// src/components/TeamCreation/UserTable.jsx
import React from 'react';

const UserTable = ({ users, teams, onUserUpdate }) => (
  <div className="user-table">
    <p>📋 User Table placeholder</p>
    <button onClick={onUserUpdate}>Refresh Users</button>
  </div>
);

export default UserTable;
